node_modules/
.env
# config ทั้งหมด (มี token = secret) — เก็บในเครื่อง ห้าม commit
config.json
*.log
dist/
# standalone exe ที่ build จาก bun --compile (ไฟล์ใหญ่ ~90MB)
*.exe
*.exe.bak
.DS_Store
# channel ID ที่ bot สร้าง/จำไว้ (ผูกกับ guild + ห้องจริง ไม่ควร commit)
channels.json
# DST persistent data — default persistent root = โฟลเดอร์โปรเจกต์
# cluster_token.txt = secret, saves, backups ห้าม commit
DoNotStarveTogether/
Agreements/
backups/
